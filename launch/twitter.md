# Twitter / X

Post once the demo GIF renders cleanly in-feed (test in the composer). Keep it
to one tweet + an optional reply thread; attach `assets/demo.gif`.

## Main tweet (option A — the pain)

> Tired of scrolling 3,000 lines of JSONL to see what your coding agent actually did?
>
> agx turns the session file your agent CLI already writes into a navigable terminal timeline. One keypress shows a tool call's input and its result on the same screen.
>
> Claude Code / Codex / Gemini + 5 more formats. Pure Rust.
>
> cargo install agx-tui

## Main tweet (option B — the MCP angle)

> agx-mcp lets an AI agent step through its OWN session mid-run — "summarize what I did," "what were my recent errors," "am I calling a tool in a loop" — by reading the trace file it already writes.
>
> A debugger the agent can use on itself. Pure Rust, zero instrumentation.
>
> github.com/brevity1swos/agx

## Optional reply (thread continuation)

> Zero instrumentation: no SDK, no hosted dashboard, no telemetry. It auto-detects 8 trace formats (Claude Code, Codex, Gemini, OpenAI-generic, LangChain, Vercel AI SDK, OpenTelemetry GenAI JSON + binary) and normalizes them into one timeline with per-step tokens + cost.

## Hashtags / handles (use sparingly)

`#rustlang` · consider tagging the ratatui account if the GIF leads with the TUI.
Avoid over-tagging — one or two max.
